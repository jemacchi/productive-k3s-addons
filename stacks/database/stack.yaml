apiVersion: addons.productive-k3s.io/v1
kind: Stack
metadata:
  name: database
  version: 0.1.0
  category: database
spec:
  resolution:
    mode: catalog
  runtime:
    compatibility:
      kubernetes:
        distros:
          - k3s
          - rke2
  addons:
    - longhorn
    - cloudnative-pg
