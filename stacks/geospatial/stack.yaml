apiVersion: addons.productive-k3s.io/v1
kind: Stack
metadata:
  name: geospatial
  version: 0.1.0
  category: geospatial
spec:
  resolution:
    mode: catalog
  runtime:
    compatibility:
      kubernetes:
        distros:
          - k3s
          - rke2
  addons:
    - geoserver-cloud
