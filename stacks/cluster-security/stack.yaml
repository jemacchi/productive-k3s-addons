apiVersion: addons.productive-k3s.io/v1
kind: Stack
metadata:
  name: cluster-security
  version: 0.1.0
  category: security
spec:
  resolution:
    mode: catalog
  runtime:
    compatibility:
      kubernetes:
        distros:
          - k3s
          - rke2
  addons:
    - trivy-operator
    - kyverno
